/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: process.env.SITE_URL || 'https://www.weperch.live',
    generateRobotsTxt: true, // (optional) Generate a robots.txt file
    // Additional options can be added here
  };
  